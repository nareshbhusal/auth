import React from 'react';

const InvalidRoute = () => {
    return (
        <p style={{color: 'red', margin: '4rem', textAlign:'center'}}>
            404: Invalid route
        </p>
    );
}

export default InvalidRoute;